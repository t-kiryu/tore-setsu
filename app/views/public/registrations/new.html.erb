<div>
  <div class="title">
    <h2>新規会員登録</h2>
  </div>

  <div class="form-group">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "public/shared/error_messages", resource: resource %>

      <div class="field">
        <table>
          <thead>
            <th></th>
            <th></th>
            <th></th>
          </thead>
          <tbody>
            <tr>
              <td>氏名</td>
              <td><%= f.label :"(性)" %> <%= f.text_field :last_name, placeholder: "山田" %></td>
              <td><%= f.label :"(名)" %> <%= f.text_field :first_name, placeholder: "太郎" %></td>
            </tr>
            <tr>
              <td>フリガナ</td>
              <td><%= f.label :"(セイ)" %> <%= f.text_field :last_name_kana, placeholder: "ヤマダ" %></td>
              <td><%= f.label :"(メイ)" %> <%= f.text_field :first_name_kana, placeholder: "タロウ" %></td>
            </tr>
            <tr>
              <td><%= f.label :"ユーザーネーム" %></td>
              <td colspan="2"><%= f.text_field :user_name, placeholder: "y-taro" %></td>
            </tr>
            <tr>
              <td>メールアドレス</td>
              <td colspan="2"><%= f.email_field :email, autocomplete: "email", placeholder: "sample@example.com" %></td>
            </tr>
            <tr>
              <td>パスワード</td>
              <td colspan="2"><%= f.password_field :password %></td>
            </tr>
            <tr>
              <td>確認用パスワード</td>
              <td colspan="2"><%= f.password_field :password_confirmation %></td>
            </tr>
            <tr>
              <td></td>
              <td><%= f.submit "新規登録", class: "btn btn-primary btn-sm" %></td>
            </tr>
          </tbody>
        </table>
      </div>
    <% end %>
  </div>

  <p>登録がお済みの方は<%= link_to " こちら ", new_customer_session_path %>からログインをお願いします</p>
</div>
