<div>
  <div class="title">
    <b><%= current_customer.user_name %>さんの登録内容編集</b>
  </div>

  <%= form_with model: @customer, url: customers_infomation_path(@customer), method: :patch do |f| %>
    <div class="form-group">
      <table>
        <thead>
          <tr>
            <th></th>
            <td></td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>氏名</td>
            <td><%= f.label :"(性)" %> <%= @customer.last_name %></td>
            <td><%= f.label :"(名)" %> <%= @customer.first_name %></td>
          </tr>
          <tr>
            <td>フリガナ</td>
            <td><%= f.label :"(セイ)" %> <%= @customer.last_name_kana %></td>
            <td><%= f.label :"(メイ)" %> <%= @customer.first_name_kana %></td>
          </tr>
          <tr>
            <td>ユーザーネーム</td>
            <td colcpan="2"><%= @customer.user_name %></td>
          </tr><tr>
            <td>メールアドレス</td>
            <td colcpan="2"><%= @customer.email %></td>
          </tr>
        </tbody>
      </table>
    </div>

    <%= f.submit "変更登録", class: "btn btn-success btn-sm" %>

  <% end %>
</div>